spring:
  application:
    name: crud-task
  datasource:
    url: jdbc:mysql://localhost:3306/task1
    username: root
    password: 0000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
